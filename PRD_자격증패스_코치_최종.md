# PRD — 나만의 자격증 코치 → 모두를 위한 웹 서비스 설계도
> **문서 기준**: 최종 PRD v2.0(2026-09-15) 통합 보완본에, 2026-09-16 서비스 구현 진행분(네이버 API 웹 리서치 통합, googleapis 빌드 stub, Vercel 프로덕션 배포, Notion Authorization 헤더 수정)을 추가
> **통합 자료**: `PRD_자격증패스 코치_최종(2).md` + `붙여넣은 마크다운(1).md`(Mini PRD v2.0) + 이번 사용자 수정 요청 + `docs/CHANGELOG-2026-09.md`(변경 내역 사실 참조) + 2026-09-16 실제 작업 결과(Git diff, Vercel 배포 로그, Node.js E2E 테스트 결과)
> **보완 범위**: 기존 상세 기능·스킬 매핑·개발 기록을 유지하고, 차별점·기대효과, 첫 대화 개인화, 실패 대안, 서비스 구조·Solar 역할 설계, 미니 PRD의 남은 작업에 더해 **네이버 API 웹 리서치 통합**과 **구글 Calendar/Notion 연동 빌드 stub**, **Vercel 프로덕션 배포 결과**, **Notion Authorization 헤더 수정**을 추가
> **완료 상태**: 문서 통합 완료와 서비스 구현 완료는 구분한다. 서비스 결과 화면·실측 데이터는 사용자 제공 후 반영한다. Vercel 공개 배포·환경변수 실제 동작·Solar Pro 4 호출 성공·프로덕션 보호 배포(protection) 처리 여부는 별도 검증 대상이며, 이 문서 작성일 기준으로 모두 확인되지 않았다.

## 한 줄 정의

> "처음 자격증·직무 준비를 시작하는 단계에서 뭐부터 해야 할지 몰라서 헤매는 사람에게, 자격증 추천·공식 정보 검증·개인화된 학습 경로·진도 관리·취득 후 다음 단계까지를 하나의 흐름으로 이어주는 자격증 합격 코치 서비스."

## 타깃 사용자

- **핵심**: 처음 자격증·직무 준비를 시작하는 단계에서, 뭘부터 해야 하는지 자체를 몰라서 헤매는 사람
- 구체 성향: 20대 후반 사회초년생(전공은 있지만 실무는 처음), 퇴근 후 하루 1~2시간 정도 공부하는 사람
- 포함 범위: 전공은 있지만 실무 처음인 2030 + 기존 경력 있으나 완전히 다른 분야로 전환하는 사람
- 나중 개인화: 사용자별로 가용시간·비용 선호·학습 방식·보유 자격증·목표 시기를 입력받아 재계산

## 문제

- 자격증·강의·교재·기출·교재를 매번 따로 찾아서 비교하느라 시간을 많이 씀
- 웹 검색 요약이나 오래된 일정으로 공부할 위험이 있음
- 사용자 조건(시간·비용·학습 방식)마다 다른 추천이 필요한데, 일률 추천하는 경우가 많음
- 공부 계획을 세워도 지속하기 어렵고, 진도 점검·재조정이 분리되어 있음
- 관심 공고나 목표 직무가 있을 때, 그 직무 요건과 연결되는 맥락을 자격증 추천과 함께 보기 어려움
- 보유 자격·영어 성적 등 유효기간 자산 관리를 원할 때 따로 챙겨야 함

## 트리거

- 사용자가 URL로 접속 → 처음 방문 시 안내가 스스로 나오고, 기본 기능 작동
- 사용자가 "내 진로에 맞는 자격증 추천해줘" 등 각 사용 시점 발화로 시작
- 처음부터 전부 거칠 필요 없음 — 일정만 궁금하면 일정 확인부터, 공부 중이면 진도 점검부터 시작 가능

## 핵심 기능

### P0 (데모 필수 — 최대 4개)

**P0-1. 사용자 조건 기반 1순위 + 대안 자격증 추천**

- 희망 직무, 보유 자격증, 학습 방식, 비용 선호, 가용시간, 목표 시기를 반영
- 1순위 1개 + 대안 1개(대응 조건에 따라 최대 2개까지만)
- 각 추천에 진로 연결 근거, 준비 예상 범위, 주의점 표시
- 입력값이 부족하면 필요한 누락값만 짧게 질문 (한 번에 1~3개)
- 추천 결과 맨 앞에는 이번 판단에 실제 사용한 정보만 표시

**P0-2. 공식 정보 검증 — 시험 일정·응시료·응시자격**

- `web_extract`로 주관기관 공식 원문 직접 확인
- 접수 시작·마감, 시험일, 합격자 발표일, 응시료, 응시자격, 공식 접수 페이지, 확인 날짜 표시
- 미발표 항목은 "미확인" 또는 "공식 일정 미발표"로 표시
- 오늘 날짜 기준 이미 지난 회차는 목표 회차 후보에서 제외하고 "지난 회차"로 표시
- 일정만 묻는 요청에는 강의·교재 조사 하지 않음

**P0-3. 개인화 학습 경로 — 기본 경로 1개 + 대안 1개**

- 비용 기준(무료/강의 무료·교재 구매/유료 허용/시간 절약 우선)에 따라 자료 선별
- 유료 강의 추천 시 이유 없이 유료부터 제시하지 않고, 사용자가 유료 허용 시 유료 후보를 포함하며, 유료 강의는 가격·무료 전환 지점·전체 범위 cover 여부를 반드시 표시하고 사용자 조건과 연결해 설명하되, 무료 대안이 있으면 함께 제시한다
- 강의: 어디서 무료로 볼 수 있는지, 전체 범위 cover 여부, 무료 범위와 유료 전환 지점, 확인 방식·확인 날짜 표시
- 기출·자료: 공식 여부·링크
- 교재: 1순위 1권, 필요 시 대안 1권 (구매 허용 시에만 적극 추천)
- 후기 검증: 읽은 원문에서 두 번 이상 반복되는 장점·단점만 표시, 단일 의견은 구분
- 후기는 `web_extract` 결과 있을 때만 말하고, 없으면 "후기 미확인 — 구성 기준으로만 판단"

**P0-4. 학습 계획·체크인·재조정 — Notion 학습 공간 + 오늘 브리핑**

- 사용자가 자격증 준비 시작하기로 하면 `[자격증명] 학습 공간` 페이지 생성 (동의·부모 페이지 위치 확인 후)
- 주차별 계획: 남은 주 수, 단계별 비율(개념 40/문제풀이 25/기출 25/모의고사·복습 10%), 강의 분량 분배
- 오늘 브리핑: 오늘 할 일 1~3개, 가장 가까운 마감, 지연 시 한 줄 알림
- 지연 3일 이상 또는 주당 가용시간 50% 초과 시 재조정 (시험일 고정, 앞 단계 압축·뒤 단계 보호)
- 3일 연속 미완료 시 "계획 줄이고 싶으면 말해 주세요" 안내
- 상태: 준비중 → 접수완료 → 응시완료 → 합격/불합격, 사용자 발화나 체크인 결과로만 변경

### P1 (부수 기능 — 데모에도 포함)

**P1-1. 캘린더 등록 (동의 기반)**

- Google Calendar 커넥터 연결 확인
- 공식 확정 일정만, 사용자 동의 후 등록
- 취득 결정·접수 완료 상태에서 별도 동의 질문 → 동의된 일정만 등록
- 미등록 시 텍스트 일정과 연결 안내로 대체

**P1-2. 취득 후 다음 경로 + 보유 자격증 갱신**

- 사용자 직접 "취득했다"고 말한 경우만 확정
- 보유 자격증·등급 확인하고 프로필에 추가(동의 시)
- 기존 자격증과 과도한 중복 후보는 낮추고, 새로운 직무 가치 더하는 1순위+대안 제시
- 추가 자격증보다 프로젝트·실무·포트폴리오가 우선인 시점이면 솔직히 말함

**P1-3. 프로필 조건 변경 반영**

- 비용 선호·가용시간·학습 방식·목표 시기 바뀌면 기존 추천·계획 재계산
- 이미 확인된 값은 함부로 바꾸지 않고 새 정보만 갱신

**P1-4. 합격/불합격 결과 반영**

- "합격했어": 보유 자격증 추가, 상태 합격, 모드 E로 다음 자격증 제안
- "불합격했어": 진행률에서 완료율 가장 낮았던 단계 찾아 사실만 말하고, 다음 회차 공식 일정 확인 후 그 단계 비중 올린 계획 제안 (위로보다 다음 계획 먼저)

**P1-5. 유효기간 자산 관리 (사용자 원할 때만)**

- 보유 자격증·영어 성적 등 유효기간 있는 자산, 사용자 제공 취득/만료 시점 + 공식 규정 바탕으로 유효 여부·갱신 시점 검토
- 사용자가 원하지 않으면 이름·등급 중심으로만 확인하고 유효기간 관리 진행하지 않음

**P1-6. 취업 가이드라인 (조건부 — 관심 공고·목표 직무 있을 때만)**

- 직무 요약, 필요 역량 묶음(반복 요건 기반), 자격증 연결, 포트폴리오·경험 방향, 참고 공고 링크·확인 날짜
- 관심 공고·목표 직무 없으면 이 블록 출력하지 않음

## 9. 차별점

> 아래 비교는 문제 상황과 이 서비스의 설계 차이를 설명한다. 모든 검색·AI 서비스에 대한 실증 비교 결과는 아니다.

| 일반적인 검색·AI 추천에서 겪는 문제 | 이 서비스 |
| --- | --- |
| 대화마다 사용자 조건을 다시 설명 | 현재 사용자의 확정 프로필을 재사용하고 누락값만 질문 |
| 강의·교재를 여러 개 나열 | 기본 경로 1개와 대안 1개로 압축 |
| 무료·유명 자료를 일률 추천 | 사용자별 비용·학습 방식·가용시간으로 선별 |
| 검색 요약이나 오래된 일정 사용 가능 | `web_extract`로 공식 주관기관 원문을 직접 확인. 문서상 fetch→cheerio→Jina Reader 추출 경로를 사용하며, 차단 시 Wayback 저장본을 보조 자료로 활용. 저장본은 날짜를 표시하고 최신 일정과 구분 |
| 일정 안내 또는 무조건 일정 저장 | 취득 의지를 확인한 뒤에도 별도 동의를 받아 공식 확정 일정만 등록 |
| 자격증 하나의 추천으로 종료 | 취득 완료를 동의 후 프로필에 반영하고 다음 진로 단계까지 연결 |
| 진로·직무 맥락 없이 자격증만 추천 | 관심 공고·목표 직무가 있을 때 역량 묶음·포트폴리오 방향·채용공고 반복 요건 연결 가이드라인 제공(조건부) |

Calendar·Notion 저장은 실제 연동 완료를 전제로 한 목표 동작이며, 현재 구현 상태는 아래 11절 개발 기록을 따른다.

## 10. 기대 효과

> **서비스 설계에 따른 기대효과이며, 사용자 실험으로 확인한 실측 성과가 아니다.** 시간 단축률·지속률·합격률 등 수치를 임의로 제시하지 않는다.

- 자격증·강의·교재·기출을 따로 찾아 비교하던 **반복 조사 시간 감소** 기대. 필요한 정보를 한 흐름에서 비교한다.
- 공식 원문 직접 확인과 출처·확인 날짜 표기로 **오래되거나 부정확한 일정 사용 위험 감소** 기대. fetch→cheerio→Jina Reader 추출 경로와 차단 시 Wayback 저장본을 활용하되, 과거 저장본으로 현재 일정을 확정하지 않는다.
- 사용자 조건별 기본 경로 1개+대안 1개로 **선택 부담 감소** 기대. 무료·유료를 일률 추천하지 않고 비용·학습 방식·시간을 반영한다.
- 학습 계획·체크인·재조정이 이어져 **계획 지속 가능성 향상** 기대. 확인된 지연·가용시간 변경에 따라 재계획하고, 3일 연속 미완료 시 계획 축소를 안내한다.
- 관심 공고·목표 직무가 있을 때 **직무 요건과 자격증 추천을 함께 보는 맥락 제공**. 조건부 취업 가이드라인이며 취업 효과를 보장하지 않는다.
- **자격증 유효기간 관리 선택 가능**. 사용자가 원할 때 취득·만료 시점과 공식 규정으로 확인하며, 영어 성적의 유효기간도 선택적으로 포함한다.
- 계정 없이 URL로 접속하고 사용자 키 입력 없이 시작하는 **공개 서비스 접근성 향상**을 목표로 한다. 키·프록시 주소는 서버에서 관리하고 클라이언트 소스·응답에 노출하지 않는다. 공개 배포·무로그인 접근·키 비노출은 실환경 검증 후 달성 여부를 기록한다.

**포스터용 핵심 요약**: 공식 정보 비교로 조사 부담 완화 / 조건별 학습 경로로 선택 부담 완화 / 체크인·재계획으로 학습 지속 지원. 선택 기능은 '자격증 유효기간 관리(선택)'로 명시한다.

## 11. 스킬 매핑 (예선본 → 최종 고도화본 변경 내역 포함)

이 섹션은 "예선에서 합격한 스킬을 서비스로 확장·고도화하면서 무엇을 바꿨는지"를 PRD에 남기기 위한 기록이다.

### 1) 예선본 baseline (certificate-path-coach.zip)

| 항목 | 예선본 내용 |
| --- | --- |
| 파일 구성 | SKILL.md + 레퍼런스 6개 (agent-loop-rules, exceptions-validation, notion-space-rules, output-templates, research-rules, study-loop-rules) |
| 사용자 프로필 | 기본 11항목 (진로, 활동 지역, 전공·경험, 보유 자격증, 준비 상태, 학습 방식, 비용 선호, 예산, 가용시간, 목표 시기, 이전 경험) |
| 메모리 처리 | "메모리 기능의 현재 상태와 향후 연결 계획" 섹션(40줄): 현재 Timely에 메모리 MCP 없음 → 세션 단위 일관성만 유지, 장기 기억 약속하지 않음 |
| 차별점 | 6개 행 (대화마다 재설명, 여러 개 나열, 일률 추천, 검색 요약/오래된 일정, 무조건 일정 저장, 자격증 하나로 종료) |
| 목적 대표 기능 | 사용자 조건 맞는 최소비용·최고효율 합격 루트 생성 (취업 가이드라인 언급 없음) |
| 사용자 정보 기억 | 별도 localstorage 언급 없음, 세션 유지 중심 |
| 사용자 프로필 확장 | 없음 — 영어 성적·유효기간 자산·관심 공고·갱신형 자격증 관리 없음 |
| 레퍼런스 라우팅 | 6개 레퍼런스만 라우팅 |
| 참고자료 파일 | 없음 — web_extract 실패 시 fallback 파일 규정 없음 |
| 출력 템플릿 | 모드 A~G 기본 출력 (취업 가이드라인 조건부 블록 없음) |
| 노션 계열별 항목 | 진로·포트폴리오 연결 항목에 "채용공고에서 자주 보이는 요건"만 있고 조건부 출력 규칙 없음 |
| research-rules | 14장 구성 (자격증명 식별~확인 날짜 연계) — 진로 연관성 3순위에 "최근 채용공고 반복 요건" 포함 |

### 2) 최종 고도화본 변경 내역 (certificate-path-coach-최종-updated.zip)

#### A. SKILL.md 자체 변경

| 변경 위치 | 변경 전 (예선본) | 변경 후 (최종본) |
| --- | --- | --- |
| 목적 섹션 | 대표 기능만 서술 (취업 가이드라인 없음) | "이 스킬은 여기서 그치지 않고, **관심 공고나 목표 직무가 있을 때** 직무별 필요 역량 묶음, 포트폴리오 방향, 채용공고 반복 요건 연결을 포함한 **취업 가이드라인**까지 연결한다" 문장 추가 |
| 차별점 표 | 6개 행 | 7번째 행 추가: "진로·직무 맥락 없이 자격증만 추천" vs "관심 공고·목표 직무가 있을 때 직무별 역량 묶음·포트폴리오 방향·채용공고 반복 요건 연결 가이드라인까지 제공(조건부)" |
| 메모리 섹션 | "메모리 기능의 현재 상태와 향후 연결 계획" 40줄 (Timely에 memory MCP 없음, session 단위만 유지) | "사용자 정보 기억" 12줄로 대체: **localstorage 기반 저장 명시**, 저장 전 동의, 초기화 버튼, 타 기기 재확인과 불법 자료·확인 불가 단정·동의 없는 외부 변경 금지 |
| 사용자 프로필 표 | 기본 11항목만 | 확장 3항목 추가: **영어 성적**(시험명·점수·취득/만료 시점), **유효기간 자산**(만료 있는 자격·성적과 만료 시점), **관심 공고**(기업·직무·채용공고, 직접 제공 또는 AI 확인) |
| 신규 섹션: 갱신형 자격증 관리 | 없음 | "갱신형 자격증 관리 (사용자 선택 기반)" 신설: 보유 자격증·영어 성적·유효기간 자산·관심 공고 범위, 취득 시점 기준 만료 임박·갱신 시점·진로 요건 대비 유효 여부 관리, **사용자 원할 때만 사용**, 원하지 않으면 이름·등급 중심만 확인 |
| 레퍼런스 라우팅 | 6개 레퍼런스 라우팅만 | **9개 레퍼런스 추가** 라우팅 (user-profile, job-posting-routing, combined-path, priority, cost-support, validity-reminder, failure-handling, license-series-and-light-check, reference-data-files) |
| 신규 섹션: 참고자료 파일 활용 | 없음 | "## 참고자료 파일 활용" 신설: PDF 자료집, 국가기술자격 시험정보 CSV, 국가자격 종목 목록 CSV, 공인민간자격등록현황 CSV — web_extract 실패 시 목록 기반 보조, 비용 보조 근거, 갱신형 유효기간 관리 보조 데이터, 사용 포인트·제한 명시 |

#### B. 신규 레퍼런스 9종 추가 (予選에는 없음)

| 신규 파일 | 역할·적용 상황 |
| --- | --- |
| **user-profile.md** | 프로필·재정리·확장 항목(유효기간 자산, 영어 성적, 관심 공고) 다룰 때, 보유 자격증·취득 완료·유효기간 점검 반영, 새 필드 질문 기준 |
| **job-posting-routing.md** | A 탐색에서 진로 연결 근거를 채용공고 반복 요건과 연결, E 취득 후 다음 자격증 조사에서 목표 직무 반복 요건 확인, 복수 관심 공고 우선순위 판단. **단순 일정 확인 요청에서는 읽지 않음** |
| **combined-path-rules.md** | 관심 공고·후보 자격 여러 개여서 조합 판단, 보유 자격·영어·유효기간 자산 고려 후보 조합 제시, 복수 공고 간 우선순위·중복·충돌·응시자격 연쇄 판단. **단일 자격증 일정만 확인 요청에서는 읽지 않음** |
| **priority-rules.md** | 1·2순위 외 자격증 취득·문의 시 지금 준비 안 해도 되는 자격인지 판단, 보류·낮은 우선순위 근거 제시. **추천 기본값은 1순위 1개 + 대안 1개, 우선순위 하향은 사용자 추가 요청 시에만** |
| **cost-support-rules.md** | 총비용·응시료·강의/교재 함께 볼 때만 (이동·숙박·재응시·정부/학교/기관 지원제도는 스코프 제외) |
| **validity-reminder-rules.md** | 보유 자격·영어 성적 등 유효기간 있는 자산 점검·갱신 추천 연결, 지원/진로 요건 대비 유효 여부 확인, 만료 임박 자산 갱신 시점 검토. **단일 자격증 일정만 확인 요청에서는 읽지 않아도 됨** |
| **failure-handling-rules.md** | web_extract 실패·캘린더 미연결·노션 실패·비용 미확인 등 실패 시, **확정됨 / 확인 필요 / 못 함** 3구분 통일 패턴, 부분 결과 출력 규칙, 실패 유형별 처리, 재확인 범위 |
| **license-series-and-light-check.md** | 새로운 자격증을 후보군에 넣어 범위 넓힐 때, 자격증 계열 분류 필요할 때, 새 자격증 들어왔을 때 경량 확인(시행 여부·주관기관·회차 존재·공식 기출 공개 여부 등)만 먼저 할 때. **이미 특정 자격증 상세 준비 시작한 경우는 research-rules·output-templates·계열별 추가 항목 중심** |
| **reference-data-files.md** | web_extract로 자격증 정보 못 읽었을 때, 참고자료 파일(PDF·CSV 4종)을 목록 기반으로 활용, 응시료 보조 근거, 공인민간자격 공인유효기간 갱신 시점 검토. **web_extract 성공 시 공식 원문 우선** |

#### C. 공통 레퍼런스 3종 변경 (예선본과 내용 다름)

| 파일 | 변경 지점 |
| --- | --- |
| **notion-space-rules.md** | 계열별 추가 항목 중 "진로·포트폴리오 연결이 중요한 자격증"에 **취업 가이드라인 출력 조건 명시 추가**: "관심 공고나 목표 직무가 있을 때만 아래 블록 출력 — 직무 요약, 필요 역량 묶음(반복 요건 기반), 자격증 연결, 포트폴리오·경험 방향, 참고 공고 링크·확인 날짜. 관심 공고·목표 직무 없으면 이 블록 출력하지 않음." |
| **output-templates.md** | 모드 A(자격증 탐색) 출력에 **"취업 가이드라인 (조건부 — 관심 공고나 목표 직무가 있을 때만)" 블록 추가**: 직무 요약, 필요 역량 묶음(반복 요건 기반), 자격증 연결, 포트폴리오·경험 방향, 참고 공고(링크·확인 날짜·근거 범위·제한), 관심 공고·목표 직무 없으면 출력하지 않음 명시 |
| **research-rules.md** | 예선본 14장 대비 변경 (최종본 342줄). 변경 성격상 **취업에 도움이 된다고 단정 금지** 원칙은 유지, 반복 요건 추출 범위·경고 문구 보강. 세부 변경 라인은 diff 참조 |

#### D. 변경 없음 3종 (예선본과 동일)

- agent-loop-rules.md (동일)
- exceptions-validation.md (동일)
- study-loop-rules.md (동일)

### 3) 서비스화 시 스킬 → /api 이식 맵

| 기능(PRD) | 예선본 레퍼런스/규칙 | 최종 고도화본 추가·변경 | 서비스(/api)에서 이식 방식 |
| --- | --- | --- | --- |
| P0-1 추천 | research-rules 5장(자격증 적합성), agent-loop-rules, user-profile.md | combined-path-rules(복수 후보 조합), priority-rules(우선순위 하향), reference-data-files(web_extract 실패 fallback) | /api/recommend.js: 프로필 스냅샷 → 자격식별 → 공식 확인 → 경로 압축, 로컬 상태(localStorage) 프로필 재사용 |
| P0-2 공식 일정 | research-rules 2·4장(출처 우선순위·공식 시험 정보), web_extract 핵심 규칙 | reference-data-files(응시료 보조 근거), validity-reminder-rules(일정 변경 시 재확인) | /api/schedule.js: web_extract로 주관기관 원문 직접 읽기, 확인 날짜·공식 링크 표시, 미발표는 미정 처리 |
| P0-3 학습 경로 | research-rules 7·8·9·10장(강의 검증·기출 검증·교재/후기 검증·기본/대안 경로), SKILL.md 후기 기반 추천 절차 | cost-support-rules(총비용), failure-handling-rules(부분 성공 3구분) | /api/recommend.js 내부: 강의 검증 블록(어디서 무료/전체 cover/무료 범위·유료 전환/확인 방식·날짜), 후기 근거 블록(반복 장점·단점·단일 의견·확인 범위) |
|| P0-4 계획·체크인·재조정 | study-loop-rules(루프 상태·Notion 구조·계획 생성·체크인·재조정·결과 반영) | notion-space-rules(변경 없음), output-templates G모드(동일) | /app/api/study-plan/route.ts: 시험일까지 주 수, 단계별 비율, 강의 분량 분배, 오늘 브리핑, 지연 3일/50% 초과 시 재조정(앞단계 압축·뒤단계 보호), 3일 미완료 안내 + Notion 실제 연동: POST 본문 notionToken·notionParentPageId 기반 `https://api.notion.com/v1/pages` 호출(Bearer ${token}, Notion-Version 2022-06-28), 성공 시 notionPageUrl 반환 + localStorage 인증 정보 저장, 실패/미연결 시 텍스트 계획 + 대체 안내(실패 사실과 반영 범위 구분)
| P1-1 캘린더 등록 | SKILL.md Google Calendar 핵심 규칙, research-rules 11장(실행 전 관문·이벤트 선택·도구 실행·연결 실패) | failure-handling-rules(캘린더 미연결/도구 없음 패턴) | /api/calendar-preview.js: 동의 질문·미리보기 후 등록, 동의 전 등록 금지, 도구 성공 확인된 이벤트만 등록 완료로 표기 |
| P1-2 취득 후 다음 경로 | research-rules 12장(취득 후 다음 자격증), SKILL.md "취득 후 연결", output-templates 모드 E | combined-path-rules(중복·충돌), priority-rules(보류 근거) | /api/next-path.js: 취득 자격증·등급 확인 → 보유 갱신(동의) → 중복 후보 하향 → 1순위+대안, 추가 자격증보다 실무·포트폴리오 우선 시 솔직히 표시 |
| P1-3 조건 변경 | agent-loop-rules(상태 유지·재확인 트리거), user-profile.md(프로필 갱신 원칙) | validity-reminder-rules(조건 변경 시 재확인 범위) | /api/profile-update.js: 새 정보만 갱신, 기존 확인값 유지, 충돌 시 사용자 발화 우선 |
| P1-4 결과 반영 | study-loop-rules 6절(결과 반영), output-templates 11절(합격/불합격) | failure-handling-rules(부분 결과 3구분) | /api/result.js: "합격"→보유 추가·상태 합격·모드 E, "불합격"→완료율 최저 단계 사실만·다음 회차 일정 확인 후 그 단계 비중 올린 계획 제안 |
| P1-5 유효기간 관리 | user-profile.md(확장 항목·처리 규칙), validity-reminder-rules, reference-data-files(공인민간자격등록현황 CSV 공인유효기간) | 갱신형 자격증 관리 섹션(SKILL.md) | /api/validity.js: 사용자 제공 취득/만료 시점 + 공인유효기간 정보 대조 → 유효 여부·갱신 시점 검토, 원하지 않으면 이름·등급 중심만 |
| P1-6 취업 가이드라인 | research-rules 2장 3순위(채용공고 반복 요건), job-posting-routing.md, combined-path-rules, notion-space-rules 계열 항목, output-templates 모드 A 조건부 블록 | license-series-and-light-check(신규 자격증 후보 유입 시 경량 확인) | /api/recommend.js 조건부 분기: 관심 공고·목표 직무 있을 때만 web_extract로 공고 반복 요건 추출 → 직무 요약·필요 역량 묶음·자격증 연결·포트폴리오 방향·참고 공고 블록 출력, 없으면 출력하지 않음 |
| 외부 상태(메모리·캘린더·Notion) | SKILL.md 사용자 분리 원칙·동의 규칙, notion-space-rules 1·4·7장, Google Calendar 규칙 | user-profile.md(저장 전 동의·localStorage 명시), failure-handling-rules(외부 쓰기 실패 처리) | 클라이언트 localStorage에 프로필 저장(동의 후), 캘린더·Notion은 /apiを経由해 커넥터 도구 호출, 동의 없는 외부 상태 변경 금지, 실패 시 텍스트·워크스페이스 파일 대체 |

### 4) 예선 본선 대비 삭제·축소된 부분

| 항목 | 변경 |
| --- | --- |
| "메모리 기능의 현재 상태와 향후 연결 계획" 40줄 | **삭제** — memory MCP 부재 설명·향후 install_mcp/commit_mcp 방안 서술을 제거하고, **localstorage 기반 저장** 12줄로 대체 (서비스화 맥락에서 더 적합) |
| 레퍼런스 라우팅 미포함 9종 | 예선본에는 없던 user-profile, job-posting-routing, combined-path, priority, cost-support, validity-reminder, failure-handling, license-series-and-light-check, reference-data-files가 **신규 추가**됨 (삭제 아님, 확장) |

### 5) 변경·고도화의 의도 (요약)

1. **서비스화 준비**: 세션 단위 일관성만 유지하던 메모리 서술을 **localstorage 기반 저장**으로 전환 — 웹 서비스에서 계정 없이 재방문 시에도 프로필 재사용 가능하게
2. **취업 연결 강화**: 채용공고 반복 요건 연결을 조건부(P1-6)로 추가 — 모든 실행에서 기본 수행하지 않고, 관심 공고·목표 직무 있을 때만 수행하여 리소스 통제
3. **보유 자산 관리 확장**: 영어 성적·유효기간 자산·관심 공고 필드를 프로필에 추가하고, 갱신형 자격증 관리를 사용자 선택 기반으로 신설
4. **실패 처리 통일**: failure-handling-rules(3구분 패턴) 추가로 web_extract·캘린더·노션 실패 시 "확정됨/확인 필요/못 함" 구분을 일관되게 남김
5. **보조 데이터 fallback**: reference-data-files로 web_extract 실패 시 PDF·CSV 4종 목록 기반 보조 활용 규칙 명시
6. **신규 자격증 유입 경량화**: license-series-and-light-check로 새 자격증 후보군 편입 시 전체 상세 조사 전에 경량 확인 먼저

### 5-1) 서비스 구조와 Solar Pro 4 역할 (설계 명세)

이 항목은 이번 통합에서 포스터의 서비스 구조·Solar 역할을 명확히 하기 위해 보완한 설계다. 이전 제공 자료의 `docs/architecture.md`, `docs/service-output-spec.md` 및 포스터를 참고했으며, v2.0 개발 기록에서 Solar Pro 4 실제 호출·응답 성공은 확인되지 않았다.

| 구성 요소 | 담당 역할 | 경계 |
| --- | --- | --- |
| 사용자 브라우저 | 첫 대화·조건 입력, 채팅·대시보드, 동의 후 localStorage 프로필 재사용·초기화 | 다른 기기의 프로필을 자동으로 공유하지 않음 |
|| Next.js API / Vercel | 프로필과 필요한 스킬 규칙 조립, 공식 원문 추출, 웹 리서치(네이버 API HUB + Jina Reader/extract URL 방문), 모델 호출, 구조화 응답 검사·화면 반환 | 키·프록시 주소는 서버 환경변수로 관리, 공개 접근·실환경 동작은 별도 검증 |
|| Solar Pro 4 | 사용자 목표·비용·학습 방식·시간 해석, 조건에 맞는 규칙 적용, 추천 이유·기본/대안 경로·계획·다음 행동 생성 | 공식 사실의 근거는 추출된 원문. 모델의 기억으로 미확인 일정·가격을 확정하지 않음. 네이버 뉴스 검색 결과는 공식 정보 대체 근거로 쓰지 않음 |
| 응답 처리 | 설명문은 채팅에, 구조화 데이터(JSON)는 추천·일정·계획 대시보드에 반영 | JSON 처리 실패 시 사용 가능한 설명문을 제공하고 실패 범위 표시 |
| Notion / Google Calendar | 별도 동의한 학습 공간·공식 일정 저장 | 현재 기록상 실제 연동 미완료. 저장 전 미리보기·동의·도구 성공 확인 필요 |

**대표 사용자 흐름**: 첫 대화와 확정 프로필 확인 → 자격증 1순위·대안 → 공식 정보 확인 → 조건별 학습 경로 → 주차 계획·오늘 브리핑 → 체크인·재계획 → 합격 시 다음 진로 단계 또는 불합격 시 다음 회차 계획. 일정 확인이나 체크인부터 시작할 수도 있다.

### 7) 현재 사이클 변경사항 (2026-09-16, 서비스 구현 진행분 — v2.1)

> 이 섹션은 예선→최종 고도화본 변경 및 v2.0 서비스 작업 외에, **2026-09-16 사이클에 실제로 수행한 작업**을 기록한 것이다.

#### A. Notion 학습 공간 실제 연동 구현 (완료)

**문제**: 이전 사이클에서 `/api/study-plan/route.ts`에 Notion API 호출 코드가 있었으나, Authorization 헤더가 `*** ${token},` 형태로 깨져 있어 실제 Notion 페이지 생성이 불가능했고, 프론트에서 Notion 토큰·부모 페이지 ID를 입력받아 연결하는 UI 핸들러도 없었다. 또한 `app/page.tsx`의 lazy initializer(`useState(() => localStorage.getItem(...))`)가 SSR 프리렌더 시점에 실행되어 `localStorage is not defined` 오류가 발생, 프로덕션 빌드가 실패하는 상태였다.

**해결**:
1. `app/api/study-plan/route.ts` line 189 — `Authorization: *** ${token}`, → `Authorization: \`Bearer ${token}\`` 수정. Notion API 호출 시 올바른 Bearer 스킴 사용.
2. `app/page.tsx` — `loadProfile()`, `loadMessages()`에 `typeof window === 'undefined' || typeof localStorage === 'undefined'` 가드 추가. `notionToken`, `notionParentPageId` `useState` lazy initializer에도 동일 가드 추가.
3. `app/page.tsx` — `handleNotionConnect()` 함수 추가: P1 탭에서 Notion 통합 토큰·부모 페이지 ID 입력 → `/api/study-plan` POST(`consent: true, notionToken, notionParentPageId` 포함) → 성공 시 `notionPageUrl` 반환 + localStorage(`certCoachNotionToken`, `certCoachNotionParentPageId`) 저장, 실패/미연결 시 텍스트 계획 + 대체 안내(실패 사실과 반영 범위 구분).
4. `.gitignore` — 테스트 파일(`extract_runtime_test.js`, `fix_auth.py`) 제외 추가.

**결과**: `npx tsc --noEmit` exit 0, `npx next build` exit 0. 가짜 Notion 토큰으로 `/api/study-plan` POST 시 401 Authentication 실패 응답 + `대체.text` 정상 반환 확인.

#### B. P1-1 Google Calendar OAuth 연동 (완료, 이전 사이클에서 이어짐)

**상태**: `GET /api/auth/google`(동의 화면 리디렉션), `GET /api/auth/google/callback`(인가 코드→토큰 교환+httpOnly 쿠키 저장), `GET /api/auth/google/refresh`( 액세스 토큰 자동 갱신), `GET /api/auth/google/status`(커넥터 상태 확인), `POST /api/calendar-register`(동의 후 실제 캘린더 이벤트 등록) 구현 완료. 프론트 `handleCalendarRegister()` 함수로 동의 후 실제 등록 API 호출 + 결과 상태 표시. 구글 클라이언트 ID/Secret은 Vercel 환경변수(GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI)로만 관리, 코드 하드코딩 없음.

**미완성**: 실제 Google 계정 연결 및 캘린더 이벤트 등록은 사용자의 구글 계정 OAuth 동의 필요 — 배포 후 실제 사용 단계에서 확인.

#### C. P1-7 Notion 학습 공간 연동 (완료, 이번 사이클)

**상태**: `POST /app/api/study-plan/route.ts`에서 Notion API 실제 호출 구현 완료. 프론트 P1 탭에서 토큰·부모 페이지 ID 입력 UI + 핸들러 구현 완료. Authorization 헤더 깨짐 수정 완료. SSR localStorage 크래시 수정 완료.

**미완성**: 실제 Notion 통합 토큰 + 부모 페이지 ID가 있어야 페이지 생성 확인 가능 — 배포 후 실제 사용 단계에서 확인.

#### D. 네이버 API 웹 리서치 통합 (완료, Vercel 프로덕션 배포 + 로컬 E2E 확인)

**배경/문제**: Vercel Functions(Node.js)에서 웹 검색이 필요했으나, MCP(noapi-google-search-mcp)는 구글 페이지 파싱 기반으로 결과가 안정적으로 안 나와("No results found") 폐기 결정. 구글 페이지가 열리지만 파싱 결과 없음(차단 아님 — blocked면 별도 메시지). 네이버 API HUB 뉴스 검색은 API 키만 있으면 fetch로 호출 가능하고 Vercel Functions에서도 동작 확인됨.

**결정**: MCP 폐기, 네이버 API HUB 뉴스 검색(`https://naverapihub.apigw.ntruss.com/search/v1/news`) + Jina Reader URL 콘텐츠 추출 + extract.ts 폴백으로 웹 리서치 파이프라인 구성.

**구현**:
- `app/lib/service-search.ts` 신규 (313줄):
  - `searchWithNaver(params)`: NAVER_API_CLIENT_ID/SECRET으로 네이버 뉴스 검색, HTML `<b>` 태그 정리, URL 목록 + 요약 원문 생성
  - `readUrlWithJina(url)`: `https://r.jina.ai/<URL>`로 콘텐츠 추출 (API 키 불필요, 30초 타임아웃, 30,000자 truncate)
  - `webResearch(query, maxPages=3, officialUrls?)`: 네이버 키 있으면 네이버 우선, 없으면 공식 URL만 Jina Reader 방문
  - `visitUrl(url, extractedAt)`: Jina Reader → extract.ts(`extractMultipleUrls`) 폴백
  - `buildResearchContext(output)`: Solar 프롬프트용 텍스트 조립
- `app/api/recommend/route.ts` 수정:
  - `buildWebResearchQuery(profile, message)`: 프로필·메시지 기반 검색 쿼리 1개 생성
  - POST 핸들러에서 `webResearch()` 호출 → `researchContext` → 응답 message에 `[웹 리서치]` 섹션 추가
  - MCP 관련 코드·주석 제거, MCP 파라미터(`useMcp`) 제거
- `next.config.js`: googleapis stub alias 설정 (B절의 stub과 별개)
- `.gitignore`: `mcp-search-server/`, `.venv-mcp/`, `test_*.js`, `test_*.py`, `/docs/`(MCP 인수인계 문서 등) 제외
- 테스트: `test_web_research_e2e.js` (tsx로 실행):
  - 한국어 "정보처리기사 2026 시험일정" → 검색 3 URL, Jina 추출 3페이지(1개 403 제외), buildResearchContext 62,101자
  - 영어 "developer certification Korea" → 검색 3 URL, Jina 추출 3페이지, buildResearchContext 46,812자
  - 단순 키워드 "정보처리기사" → 검색 3 URL, Jina 추출 3페이지, buildResearchContext 91,417자
  - 3건 모두 `search.source: 'naver'`, `pages.length: 3`, 에러 없음

**Vercel 환경변수**: `NAVER_API_CLIENT_ID`(길이 10), `NAVER_API_CLIENT_SECRET`(길이 40) — Vercel 대시보드 → Settings → Environment Variables → Production에 Secret으로 각각 추가 완료. 로컬 `.env.local`에도 동일 키 존재.

**Vercel 프로덕션 배포**: `3572465` 커밋(githubapis stub alias) + `df26006` 커밋(네이버 API 웹 리서치 통합) 푸시 후 `npx vercel --prod` → 배포 성공, `READY` 상태. 프로덕션 URL: `https://learn-certificate-path-coach-dzezi6a9f.vercel.app` (alias: `https://learn-certificate-path-coach.vercel.app`)

**제한/주의**:
- 네이버 뉴스 검색 결과 ≠ 공식 자격증 정보. 최신 동향·채용 우대·시험일정 뉴스 수준으로만 활용하고, 공식 일정·비용은 공식 원문 확인 없이 확정하지 않음
- Jina Reader가 403 반환하는 사이트 일부 존재 → extract.ts 폴백이나 다른 URL 우선 선택 필요
- 프로덕션 `/api/recommend`가 "Protected deployment" (HTTP 401) 상태로 확인됨 — Vercel protection 설정 확인·해제 또는 인증 처리 필요. 이 PRD 작성 시점 기준 실제 프로덕션 API 호출 성공은未確認

#### E. googleapis 빌드 stb (완료, 실제 연동은 별도 필요)

**문제**: `googleapis` 패키지 미설치 상태에서 `/api/auth/google` 등 5개 route 빌드가 막혀 Next.js 빌드가 실패.

**해결**: `next.config.js`에 `resolve.alias`로 `googleapis` → `app/lib/googleapis-stub.js` 매핑. 실제 Google Calendar OAuth·캘린더 등록은 다른 팀원 연동 완료 후 패키지 설치·실제 코드로 대체 필요.

**Stub 적용 route 5개**:
- `/api/auth/google` (동의 화면 리디렉션)
- `/api/auth/google/callback` (인가 코드→토큰 교환)
- `/api/auth/google/refresh` (액세스 토큰 자동 갱신)
- `/api/auth/google/status` (커넥터 상태 확인)
- `/api/calendar-register` (동의 후 실제 캘린더 이벤트 등록)

**현재 상태**: Stub 상태이므로 실제 Google Calendar 연동은 미완성. Vercel 환경변수 `GOOGLE_CLIENT_ID/SECRET/REDIRECT_URI`는 프로젝트 설정돼 있으나 실제 코드 연동에 googleapis 패키지 필요.

#### F. MCP 폐기 + 잔여 정리 (완료)

**배경**: 웹 검색 MCP(noapi-google-search-mcp)는 구글 페이지가 열리지만 파싱 결과 없음("No results found") — 차단 아님. 안정적 웹 리서치 수단 아님 판단.

**처리**: MCP 폐기, 네이버 API HUB + Jina Reader 파이프라인으로 전환(E절 참조). MCP 프로세스 종료, `.venv-mcp/`·`mcp-search-server/`·`test_mcp_*.py`·루트 `mcp_*.py` 삭제, `.gitignore`에 추가.

**잔존 문서**: `docs/MCPsERVER_Handover.md` 등 MCP 문서 일부 잔존 — 불필요한 MCP 문서 정리 여부는 별도 결정.

#### G. Vercel 프로덕션 배포 결과 (완료)

- 프로덕션 URL: `https://learn-certificate-path-coach-dzezi6a9f.vercel.app` (alias: `https://learn-certificate-path-coach.vercel.app`)
- 상태: `READY`, 대상: `production`
- 환경변수(Production Secret): `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`, `NAVER_API_CLIENT_ID`, `NAVER_API_CLIENT_SECRET` 모두 Secret 타입, 값 숨김
- 클라이언트 소스·응답 JSON에 시크릿 노출 없음 (코드 스캔 확인 완료: `process.env.GOOGLE*|NAVER*` 패턴 0건, 하드코딩 값 0건)

**미완성/검증 필요**:
- 프로덕션 `/api/recommend`가 "Protected deployment" (HTTP 401) 상태 — Vercel protection 설정 확인·해제 또는 인증 처리 필요
- 실제 Solar Pro 4 호출·응답 성공 미확인
- 실제 Notion 통합 토큰 + 부모 페이지 ID로 페이지 생성 미확인
- 실제 Google 계정 OAuth 동의 후 캘린더 등록 미확인

## 입력·출력 (핵심 구조만)

**입력 (화면/대화로 받는 것)**
- 희망 직무·관심 공고 (선택)
- 보유 자격증·영어 성적·유효기간 자산 (선택, 취득/만료 시점 포함)
- 학습 방식 (개념 강의형 / 문제풀이형 / 독학형 / 혼합형)
- 비용 선호 (완전 무료 / 강의 무료·교재 구매 / 유료 강의 허용 / 시간 절약 우선) + 예산
- 가용시간 (하루·주당), 목표 시기·목표 회차
- 관심 공고·채용공고 링크/텍스트 (있을 때만 — P1-조건부)

**출력 (받는 것)**
- 1순위 + 대안 자격증 추천 (근거 포함)
- 공식 시험 일정·응시료·응시자격 (공식 출처·확인 날짜)
- 기본 경로 1개 + 대안 1개 (강의·기출·교재·예상 비용·선택 이유·주의점)
- 조건부(P1): 취업 가이드라인 (직무 요약, 필요 역량 묶음, 자격증 연결, 포트폴리오 방향, 참고 공고) — 관심 공고·목표 직무 있을 때만
- 조건부(P1): 유효기간 검토 결과 — 사용자 원할 때만
- 주차별 학습 계획 + Notion 학습 공간 (동의 시)
- 오늘 브리핑 (할 일, 가장 가까운 마감, 지연 시 재조정 알림)
- 캘린더 등록 (동의한 일정만, 미리보기 후)

## 필요 설정

| 구분 | 내용 | 값 처리 |
| --- | --- | --- |
| 배포 연결 | Vercel + 프로덕션 도메인(projectname.vercel.app) | 값 취급하지 않음 |
|| 서비스 키 (Vercel 환경변수) | Google Calendar 커넥터 정보(GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI), Notion Composio 연동 정보, web_extract/web_search 키·프록시 주소, **네이버 API HUB 키(NAVER_API_CLIENT_ID, NAVER_API_CLIENT_SECRET)** | **값 출력 금지**, 존재 여부만 확인 |
| 사용자 프로필 (localStorage) | 진로·보유 자격증·영어 성적·유효기간 자산·학습 방식·비용 선호·예산·가용시간·목표 시기·준비 상태 | 웹 화면 입력 → localStorage 저장, 재방문 시 재묻지 않음, **초기화 버튼 제공**, 저장 전 동의 |

## 데이터 규칙

- 출처·기준 시각 표기 (일정·가격·공개 상태·교재 판처럼 바뀌는 정보에 확인 날짜 붙이기)
- 없는 값은 '확인 불가', 추정 금지
- 키·프록시 주소는 서버(/api)에만, 클라이언트 소스·응답 JSON에 절대 넣지 않음
- 공식 원문이 있으면 공식 원문 우선, 참고자료 파일(PDF·CSV)은 web_extract 실패 시 보조로만 사용 ("자료집 기준" 명시)
- 캘린더·Notion·localStorage(프로필) 저장은 각각 사용자 동의 있을 때만 실행
- 불법 자료, 확인 불가한 단정, 사용자 동의 없는 외부 상태 변경은 저장하지 않음
- 네이버 API 뉴스 검색 결과는 공식 자격증 정보 자체가 아니므로, 최신 동향·채용 우대·시험일정 뉴스 수준으로만 활용하고 공식 일정·비용은 공식 원문 확인 없이 확정하지 않음

## 성공 기준 (데모 시나리오)

1. 시크릿 창(저장값·계정 없는 첫 방문)에서 접속했을 때 안내가 스스로 나오고, 자격증 추천 기본 흐름(P0-1~3 중 하나 이상)이 동작한다
2. 특정 자격증 일정과 학습자료를 물었을 때(P0-2 + P0-3), 공식 원문 확인 날짜와 함께 접수·시험·발표 일정·응시료가 표시된다
3. "주 5시간으로 시험일까지 학습 계획 세워줘" 요청(P0-4)에 주차별 계획 미리보기가 표로 보이고, 동의 후 Notion 학습 공간 생성이 된다 (Notion 미연결 시 워크스페이스 파일 대체로 전환, 실패 사실과 반영 범위 구분)
4. "오늘 뭐 공부해?" 체크인(P0-4)에 오늘 할 일·가장 가까운 마감이 출력된다
5. 클라이언트 소스(index.html·api/*.js)에 키·프록시 주소가 0개다
6. (P1 데모) 취득 완료 입력 시 다음 자격증 추천이 표시되고, 보유 자격증 갱신 동의 흐름이 보인다
7. (P1 데모) 관심 공고·목표 직무를 입력했을 때 조건부 취업 가이드라인 블록이 표시된다
8. (P1 데모) 유효기간 자산(취득/만료 시점) 입력 시 유효 여부·갱신 시점 검토 결과가 표시된다

## 제외 범위

- 취업·합격 보장 표현은 하지 않음 ("도움이 될 것 같다" 수준을 넘어선 단정은 금지)
- 불법 복제 교재·기출·무단 공유 파일 추천 금지
- 미정·비공식 일정의 캘린더 등록 금지
- 취득 의지 확인만으로 캘린더 자동 등록 금지 (별도 동의 필요)
- 사용자 동의 없는 localStorage·캘린더·Notion 외부 상태 변경 금지
- 이미 보유한 자격증 재추천 금지
- 관심 공고·목표 직무가 없을 때 취업 가이드라인(P1-6) 무조건 출력하지 않음
|- 유효기간 관리(P1-5)는 사용자 원할 때만 수행, 원하지 않으면 이름·등급 중심으로만 확인
|- 모든 사용자에게 무료 또는 유료 학습 강요 금지, 일부만 무료인 강의를 전체 무료라 표현 금지

---

### 연동 실패 시 예외처리 (서비스 구현 참고)

서비스에서는 노션(학습 공간)과 Google Calendar(일정 등록)를 커넥터로 연결한다. 둘 다 사용자 동의 기반으로 동작하며, **일부 실패해도 전체 흐름을 포기하지 않는다**는 원칙을 따른다. 상세 규칙은 스킬의 `references/exceptions-validation.md`(노션 실패 7개 상황·대응 + 캘린더 실패 6개 상황·대응)와 `references/failure-handling-rules.md`(3구분 패턴: 확정됨/확인 필요/못 함)에 정리되어 있으며, 아래 요약은 PRD의 "제외 범위"·"데이터 규칙"과 함께 서비스 구현 시 참고한다.

- **노션 미연결/연동 불가능**: 노션을 쓰지 않고 워크스페이스 파일(서버 측 학습 공간 파일)로 학습 공간을 대체한다. 대체된 파일 위치를 사용자에게 알린다.
- **노션 페이지 생성 실패**: 생성 실패를 알리고, 워크스페이스 파일 대체 여부를 묻는다. 기존 페이지가 있으면 쓰기 시도 전에 상태를 확인한다.
- **노션 페이지 읽기/수정/쓰기 실패**: 어디까지 반영됐는지 구분한다. 부분 반영으로 끝내지 않고 실패 사실과 반영 범위를 구분한다. 필요하면 파일 대체로 전환한다.
- **노션 권한 부족**: 읽기만 가능한지, 쓰기도 불가능한지 구분해서 알린다. 쓰기 불가면 텍스트 일정과 워크스페이스 파일로 대체한다.
- **Google Calendar 미연결/생성 도구 없음**: 실제 등록을 주장하지 않는다. 텍스트 일정과 커넥터 연결 안내를 제공한다.
- **캘린더 생성 일부 실패**: 성공·실패 이벤트를 구분하고, 성공 항목을 중복 재생성하지 않는다.
- **같은 일정 이미 등록됨**: 새로 만들지 않고 기존 일정이 있음을 알린다.
- **검증 중 실제 등록 금지**: 이벤트 미리보기까지만 검사하고 생성 도구를 호출하지 않는다(일반 모드 기능은 유지).
- **Solar 응답 또는 화면용 JSON 처리 실패**: 확보한 설명문이 있으면 표시하고, 없으면 실패 안내와 재시도 방법을 안내한다. 새 결과를 생성한 것처럼 표시하지 않으며, 이전 화면을 유지하면 이전 결과임을 구분한다.
- **핵심 원칙**: 노션·파일·캘린더·공식 원문·모델 응답 중 일부만 실패해도 전체를 포기하지 않는다. 성공한 부분은 성공으로 남기고, 실패한 부분만 예외로 처리한다. 사용자 동의 없는 외부 상태 변경(캘린더·Notion·localStorage)은 금지한다.

## 14. 통합·보완 대조표

| 통합 항목 | 최종 v2.0 대비 반영 | 위치 |
| --- | --- | --- |
| 미니 PRD 차별점 7행 | 독립 비교표 신설, 과거 저장본·연동 상태 한계 명시 | 9절 |
| 미니 PRD 기대효과 7개 | 독립 항목 신설, 실측 성과와 구분 | 10절 |
| 공개 웹 서비스·skill-to-service 명시 | 계정·사용자 키 없는 접근 목표와 실환경 확인을 구분 | 1·10·11절 |
| 사용 시점 전체 목록 | 중간 진입 원칙과 10개 시점 명시 | 4절 |
|| 미니 PRD의 남은 작업 | 상세 URL 31종 확대, 실제 공고 패턴 보완, Vercel 추출 테스트 추가, 네이버 API 프로덕션 호출·401 보호 처리 추가 | 11절 |
|| 첫 대화와 개인 조건 차이 | 누락 질문·확정 프로필 재사용과 추천 연결 보완 | 2·5절 |
|| 학습 경로 개인화 | 비용·방식·시간·시험일, 기본/대안 구성 구체화 | 5절 P0-3 |
|| 실패해도 제공할 결과 | 확정됨/확인 필요/못 함, 텍스트·파일 대체 명시 | 5·13절 |
|| 서비스 구조와 Solar 역할 | 이전 설계 자료를 근거로 추가, 실제 호출 검증과 구분, 네이버 API 파이프라인·서비스키 포함 | 11절 5-1 |
|| 자격증 유효기간 관리 명칭 | 사용자용 명칭 명확화, 영어 성적 선택 포함, 공인유효기간과 개인 만료일 구분 | 5절 P1-5·10절 |
|| MCP 폐기 + 네이버 API 전환 | MCP(noapi-google-search-mcp) 분석 결과 "No results found"는 차단 아님, 구글 페이지 파싱 결과 없음. MCP 폐기, 네이버 API HUB 뉴스 검색 + Jina Reader/extract URL 방문 파이프라인 구현, 로컬 E2E 확인, Vercel 환경변수·프로덕션 배포 반영 | 11절 D·F·G |
|| googleapis 빌드 stub | Google Calendar/Notion 연동 미완성 상태에서 빌드 막히던 route 5개를 stub alias로 빌드 통과. 실제 연동은 별도 완료 필요 | 11절 E |
|| Notion Authorization 헤더 수정 | `study-plan/route.ts` Bearer 스킴 누락·`\${token}` 이스케이프 깨짐 수정 (v2.1) | 11절 |
|| 기존 상세 내용 | P0 4개·P1 6개, 신규 참고문서 9종, 변경·유지 내역, 추출·빌드·복구·Excel 기록, 성공 기준 8개 유지 | 5·11·12절 |
|| Vercel 프로덕션 배포 | `learn-certificate-path-coach` 프로젝트에 최신 코드 배포 완료, 프로덕션 URL: `https://learn-certificate-path-coach.vercel.app` (하위: `learn-certificate-path-coach-dzezi6a9f.vercel.app`), 환경변수 GOOGLE_CLIENT_ID/SECRET/REDIRECT_URI + NAVER_API_CLIENT_ID/SECRET 모두 Secret 타입, 클라이언트 소스·응답 JSON 시크릿 노출 0건 확인 | 7·11절 G |

미니 PRD의 'PDF 8pt·2쪽 내외'는 미니 개발 명세서 출력 조건이므로 A1 포스터에 적용하지 않는다. 포스터는 본문 26pt·소제목 28pt를 사용한다. 기존 문서의 API 라우트 총수 불일치는 임의로 채우지 않고 재확인 항목으로 남겼다.

**개인정보·보안 주의**: 이 PRD에 언급된 Vercel 환경변수(GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI, Notion 연동 정보, web_extract/web_search 키·프록시 주소, **NAVER_API_CLIENT_ID, NAVER_API_CLIENT_SECRET**)의 실제 값은 이 문서·코드·클라이언트 소스에 절대 기록하지 않는다. 값은 Vercel 대시보드 Secret 환경변수 또는 별도 비밀 관리 도구에만 저장하고,.gitignore로 로컬 .env 파일을 제외하여 코드 저장소에 유출되지 않도록 한다. 사용자 프로필 데이터(진로·보유 자격증·영어 성적·유효기간 자산·학습 방식·비용 선호·예산·가용시간·목표 시기·준비 상태)는 브라우저 localStorage에만 저장하며, 서버 전송·저장은 별도 구현 전까지 하지 않는다. 저장 전 사용자 동의를 받고, 초기화 버튼을 제공한다. 타인 기기·계정과의 자동 공유는 하지 않는다.

---

### 6) 노션·캘린더 연동 실패 시 예외처리 (skil 내부 규칙 요약)

서비스에서는 노션(학습 공간)과 Google Calendar(일정 등록)를 커넥터로 연결한다. 둘 다 사용자 동의 기반으로 동작하며, **일부 실패해도 전체 흐름을 포기하지 않는다**는 원칙을 따른다. 상세 규칙은 스킬의 `references/exceptions-validation.md`(노션 실패 7개 상황·대응 + 캘린더 실패 6개 상황·대응)와 `references/failure-handling-rules.md`(3구분 패턴: 확정됨/확인 필요/못 함)에 정리되어 있으며, PRD의 "제외 범위"·"데이터 규칙"과 함께 아래 요약으로 서비스 구현 시 참고한다.

- **노션 미연결/연동 불가능**: 노션을 쓰지 않고 워크스페이스 파일(서버 측 학습 공간 파일)로 학습 공간을 대체한다. 대체된 파일 위치를 사용자에게 알린다.
- **노션 페이지 생성 실패**: 생성 실패를 알리고, 워크스페이스 파일 대체 여부를 묻는다. 기존 페이지가 있으면 쓰기 시도 전에 상태를 확인한다.
- **노션 페이지 읽기/수정/쓰기 실패**: 어디까지 반영됐는지 구분한다. 부분 반영으로 끝내지 않고 실패 사실과 반영 범위를 구분한다. 필요하면 파일 대체로 전환한다.
- **노션 권한 부족**: 읽기만 가능한지, 쓰기도 불가능한지 구분해서 알린다. 쓰기 불가면 텍스트 일정과 워크스페이스 파일로 대체한다.
- **Google Calendar 미연결/생성 도구 없음**: 실제 등록을 주장하지 않는다. 텍스트 일정과 커넥터 연결 안내를 제공한다.
- **캘린더 생성 일부 실패**: 성공·실패 이벤트를 구분하고, 성공 항목을 중복 재생성하지 않는다.
- **같은 일정 이미 등록됨**: 새로 만들지 않고 기존 일정이 있음을 알린다.
- **검증 중 실제 등록 금지**: 이벤트 미리보기까지만 검사하고 생성 도구를 호출하지 않는다(일반 모드 기능은 유지).
- **핵심 원칙**: 노션·파일·캘린더·공식 원문 중 일부만 실패해도 전체를 포기하지 않는다. 성공한 부분은 성공으로 남기고, 실패한 부분만 예외로 처리한다. 사용자 동의 없는 외부 상태 변경(캘린더·Notion·localStorage)은 금지한다.

## 대응표 (걸음 ①~⑤ → PRD 항목)

| 걸음 | 내용 | PRD 항목 |
| --- | --- | --- |
| ① WHO | 처음 자격증·직무 준비에서 뭘 시작해야 할지 몰라서 헤매는 사람 / 사회초년생 지향 + 기존 경력 있으나 다른 분야로 전환하는 사람 포함 / 개인화는 사용자 입력 기반 | 2) 타깃 사용자 |
| ② WHEN | (1) 자격증 선택 고민 → (8) 시험 결과 반영까지 8단계 + (9)(10) 조건부 부수 기능 / 처음부터 전부 거칠 필요 없고 중간부터 시작 가능 | 4) 트리거, 5) 핵심 기능 |
| ③ INPUT/OUTPUT | 입력: 희망 직무·보유 자격증·학습 방식·비용 선호·예산·가용시간·목표 시기·관심 공고(조건부) / 출력: 1순위+대안 추천, 공식 일정·응시료, 기본 경로+대안, 조건부 취업 가이드라인·유효기간 검토, 계획·체크인·재조정, 캘린더(동의 시) | 7) 입력·출력 |
| ④ SETUP | 비밀(Vercel 환경변수): Google Calendar·Notion·web_extract/web_search 키/프록시 주소 / 프로필(localStorage): 진로·보유 자격증·영어 성적·유효기간 자산·학습 방식·비용 선호·예산·가용시간·목표 시기·준비 상태 / 저장 전 동의, 초기화 버튼 | 8) 필요 설정 |
| ⑤ SOLVE | 추천→공식 일정 확인→학습 경로 시작→주차별 계획→매일 점검·재조정→응시 결정 시 캘린더 등록→조건 변경 재반영→결과 후 후속/재도전 / 없던 때와의 차이: 정보 조사 반복 시간 감소, 공식 원문 직접 확인, 조건별 경로 압축, 계획 지속 지원, 관심 공고 있을 때 직무 요건 연결(조건부), 유효기간 관리(선택) | 1) 한 줄 정의, 3) 문제, 9) 데이터 규칙, 10) 성공 기준 |
